# Cosmos 3 future-strength dose v2: outcome-blind prelaunch audit

Verdict: **GO**, limited to the exact frozen cohort `cosmos3-dose-c00d81db8d910603`, manifest SHA-256 `1a37bdba796b580c905970c441aededccf4193c51fe370f0f3557eccd5a9ee7d`. This authorizes 30 states and exactly 2,760 calls. No powered output existed or was inspected when this verdict was issued.

## Evidence checked

- The evaluation manifest is frozen and selection-free: all and only the 30 middle-phase states from the archival-v7 cohort, balanced as six tasks by five environment seeds. Its state list, interventions, analysis, controls, outcomes, and claim scope are byte-for-byte unchanged from the outcome-blind pre-smoke design. Every state has the unique frozen 92-call sequence (4 native, 4 native replay, 4 none, 4 self, 4 self replay, 60 dose, and 12 midpoint replay calls).
- The 18-file snapshot checksum list has SHA-256 `126d387ccd39dc0c6e23beefac462a80670266d647463c77622da7ec5cb6a100`; all 18 hashes verify, files are mode `0444`, and directories are mode `0555`. It pins the original protocol, prospective v2 amendment, runner, dedicated server, launcher, analyzer, validator, checkpoint verifier, intervention code, attention code, and tests.
- The excluded Bagels smoke is permanently non-admitted. Its state artifact (`933569c399deed28b3b8b3e2505376769bd5c3fbb73883f5bc9eb4a84afa9ea1`) and control-only report (`797a1d4cef08c330ba6536a45a342d57be721ffbf36eab8bf93cb00476815050`) are mode `0444` and bound by the evaluation manifest. Independent raw-control checks reproduced the exact 92-call census and all `[32,8]`/256 shapes; native/self/midpoint replay, none no-op, and alpha-zero routing controls; 84 action nonwrite audits; 80 active responses and 320 active sites; zero interpolation, nonfuture, model-input clamp, and returned-velocity errors; four fixed recipient RNG/path identities; and singleton input/model fingerprints. Dose-effect values were not evaluated for this gate.
- The full 87-file, 32,937,437,706-byte checkpoint verification receipt has SHA-256 `f3615a06511d2f8259d22887c7752aff8545a0618b3c213810a82a689cf4a8e1` and records exact file set, sizes, hashes, and no symlinks.
- On the actual Cosmos server node, container `cba98ed0dc87ac9c925d94eceb91e2b5c116fc9609023210d93ee826591f0617` was independently observed running the pinned image on GPU 2 with host port 8003. Its command uses only the dedicated server SHA-256 `a6b02c876f617ef87eaa8508e2a8bcb95c48e926c5f5b58b5e3662740930771e`; the mounted upstream policy service independently hashed to the frozen `86d0d0e70faefa88a8cee8594abb3baa541ff6f65145a80eae23b1d828500f91`. The startup log records `registry_entries=0`, and there were zero evaluation files before GO. The bound empty-registry receipt is `5d07e1faa1d8a026ce0335f31412298d3b76797fa311e0f8a7f01ac6a9a0dd8f`.
- Completion utility `freeze_cosmos3_dose_output_package.py`, SHA-256 `b9a11108958ceb2a79578cc9b07afe5692679ce87683066285da6820752c3cc3`, was reviewed with no launch blocker. It remains completion-only: it refuses before the exact 30-file set, validates identity/shape/manipulation controls, post-chmod rehashes all files, and atomically writes an external inventory.

## Scope and conditions

The result may support only a graded **action-space** response under a continuously imposed all-call latent-future interpolation, subject to the prospectively frozen hierarchical tests. It is not evidence of natural mediation, physical task success, semantic planning, monotonicity absent the four adjacent-IUT gate, or necessity from a null. GO requires the exact manifest, snapshot, audit JSON, runner, analyzer, server/container topology, one sequential 30-state lane, and fail-closed no-overwrite/resume behavior described above.
