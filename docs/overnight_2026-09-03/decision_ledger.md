# Decision ledger

This ledger records decisions before their associated outcomes are inspected.

| UTC time | Decision | Evidence available at decision time | Consequence |
| --- | --- | --- | --- |
| 2026-09-03T21:51:50Z | Freeze 48 Cosmos 3 states as six tasks x eight newly generated environment seeds. | Existing 22-state study selected maximally separated donor pairs; no outcomes from the new seeds exist. | No success, separation, or visual-quality filtering is permitted. |
| 2026-09-03T21:51:50Z | Fix branch seeds to 211, 223, 227, and 229; seed 211 is recipient. | These seeds are configuration constants and were not chosen from the new states. | Every state receives the same recipient and three donor identities. |
| 2026-09-03T21:51:50Z | Make four-way donor identification the primary selection-free outcome. | Projection alone can hide orthogonal error and is aligned with the previously selected donor axis. | Projection remains secondary; distance and orthogonal error are mandatory. |
| 2026-09-03T21:51:50Z | Complete the K/V 2 x 2 with donor-to-recipient rescue. | Existing population result contains recipient-K/V suppression but no reciprocal rescue. | Sufficiency language is gated on rescue. |
| 2026-09-03T21:51:50Z | Use FastWAM Optional-IDM as the first external smoke test and LingBot-VA as the single predetermined fallback. | Optional-IDM has an explicit future-then-action path and matched first-frame path; Faster-WAM's published path does not decode a resolved future. | One switch is allowed only on a failed implementation/control gate, never on an unfavorable causal outcome. |
| 2026-09-03T21:51:50Z | Reserve new Lambda capacity as 4 + 2 + 2 H100 GPUs in Georgia. | Account has eight GPUs of headroom and the shared filesystem is region-bound. | Existing instances are not stopped or repurposed. |

## Amendments

Append amendments here before running the affected outcome. Each amendment must state whether it creates a separately labeled exploratory analysis.

| UTC time | Amendment | Evidence available at decision time | Consequence |
| --- | --- | --- | --- |
| 2026-09-03T22:08:17Z | Expand the action-space primary analysis from one fixed recipient with three donors to all 12 ordered recipient-to-donor pairs among the same four prespecified branch seeds. Retain the original fixed-recipient physical execution battery. | No intervention outcome from any of the 48 frozen states existed or had been inspected. The runner and infrastructure were still in engineering setup. | Directionality and seed sensitivity are tested without additional state selection. The 12 action arms are repeated measurements averaged within state; physical endpoints remain a prespecified secondary result. This is part of the confirmatory design, not a separately labeled exploratory analysis. |
| 2026-09-03T22:13:56Z | Move the K/V factorial from the unavailable legacy 22-state recording set to a fresh 24-state subset: the first four config-ordered environment seeds in each of the six frozen tasks. | No K/V rescue outcome had been run or inspected. The legacy summaries referenced recordings stored only on inaccessible prior instances, while the fresh cohort was already outcome-blind and scheduled for recording. | The factorial becomes task-balanced and selection-free. All action cells and replay controls are required; physical execution is limited to distinct causal cells and native references so duplicate replay controls do not consume simulator time. This is part of the confirmatory design, not a separately labeled exploratory analysis. |
| 2026-09-03T22:18:31Z | Align the umbrella FastWAM specification to the separately content-addressed implementation manifest `fastwam-6a3d693be4f3aa90`: four one-to-one video/action seed pairs, LIBERO suite names `libero_spatial` and `libero_10`, initial-state indices 0--3, and 30 settling steps. | No native FastWAM future, action, or intervention had been generated or inspected. Only unit tests, model downloads/imports, and a simulator render preflight existed. | The executable manifest and umbrella plan now name the identical frozen smoke cohort. This is a pre-outcome specification correction, not an exploratory analysis. |
| 2026-09-03T22:21:45Z | Record the exact clean Cosmos and RoboLab container image and base-image digests in the frozen runtime specification. | No frozen Cosmos outcome had been generated or inspected. Both clean images had completed building from their already pinned commits. | Runtime provenance is content-addressed before control audits and frozen interventions. The two RoboLab node-local digests are retained because independent clean builds from identical source/base pins need not be byte-for-byte identical. |
| 2026-09-03T22:21:38Z | Supersede FastWAM manifest `fastwam-6a3d693be4f3aa90` with schema-v2 manifest `fastwam-dd8450f367664a53`, expanding the no-future control from four recipient baselines to all 12 ordered donor-labeled first-frame arms. Hold recipient action noise fixed, vary the registered donor video seed, and report exact within-recipient invariance. | No FastWAM native future, action, donor intervention, or other model outcome had been generated or inspected. Upstream's first-frame path rejects future latent/cache overrides, so donor labels and video seeds can audit non-consumption without changing that route. | The v1 manifest remains preserved as superseded pre-outcome. The canonical smoke manifest is `/lambda/nfs/imagined-future/results/overnight_2026_09_03/fastwam_optional_idm_smoke_v2/manifest.json` (file SHA-256 `c872040226b259a8dd6d64b8966818a0232cdf4965dd3c24709df475469a92e7`). This is a confirmatory control correction, not an exploratory analysis. |
| 2026-09-03T22:28:11Z | Freeze an explicit task-object name for every Cosmos 3 task and require a separate task-object-position endpoint domain in addition to the existing all-rigid-object endpoint. | No fresh-state recording or frozen Cosmos intervention outcome had been generated or inspected. A final schema audit found that the implementation's existing `object` vector concatenated all rigid objects and therefore did not isolate the manipulated task object promised by the protocol. Object names were recovered from the previously used public RoboLab task manifests, not selected from new outcomes. | Every manifest state now carries its prespecified target-object name. Frozen jobs must pass it to the runner; analyses report robot, all-object, and task-object-position endpoints separately. This is a confirmatory implementation correction, not an exploratory analysis. |
| 2026-09-03T22:31:17Z | Derive the Cosmos server runtime from the frozen clean image by installing the upstream lockfile's three policy-server wheels, each by exact wheel URL and SHA-256. | No fresh-state recording or frozen Cosmos intervention outcome had been generated or inspected. The clean image completed model loading but failed before binding a socket because the optional `policy-server` dependency group was absent. | Both Cosmos servers use image `sha256:95a8933deb69f2fc73697d846f7e17066a4d4e05dc9ef85718a7ee12cfea2a8c`; the failed dependency-incomplete image remains preserved in provenance. This is a mechanical runtime repair, not a design change or exploratory analysis. |
| 2026-09-03T22:32:16Z | Add task-object-position endpoint projection to the frozen K/V factorial's explicit secondary-outcome list. | No fresh-state recording or frozen Cosmos intervention outcome had been generated or inspected. The task-object implementation correction was already frozen for the selection-free study; a config-consistency check found that the K/V secondary-outcome list had not yet named the same domain. | Both Cosmos studies now require and report the identical separate task-object-position endpoint domain. This is a confirmatory schema consistency correction, not an exploratory analysis. |
| 2026-09-03T22:46:41Z | Scale FastWAM Optional-IDM to a powered selection-free cohort containing all 40 LIBERO tasks and untouched initial-state indices 4, 5, and 6 (120 states), retaining the smoke-frozen seeds and complete eight-condition/72-arm matrix. | The complete preregistered smoke contained 8/8 states and 576/576 valid arms and passed every frozen scale criterion. All 40 official task files were then checked mechanically and each contained 50 initial states; no powered outcome existed. | Powered manifest `fastwam-813f0233b9a2c083` contains 8,640 unique registered runs. Both assigned H100s may be used with deterministic manifest-order sharding. No state, task, branch, or donor pair may be selected or removed based on model output. |
| 2026-09-03T22:48:55Z | Set each FastWAM shard's `MUJOCO_EGL_DEVICE_ID` to its physical `CUDA_VISIBLE_DEVICES` ID instead of applying device 0 to both workers. | The initial two-GPU launch failed its GPU1 import assertion before model inference; an admission audit found zero powered run records. GPU0 was stopped during model loading. A GPU1 EGL import passed after the one-line mapping correction. | Relaunch the unchanged powered manifest on both shards, preserve the failed pre-outcome log, and record launcher SHA-256 `9496923347e1b597a302d8c73e1e68b43c196e2b4a6cceeef17303a6b45e24a3`. This is a runtime repair with no design or analysis change. |
| 2026-09-03T22:59:50Z | Freeze a separate future-latent x video-cache 2x2 extension on the same 120 FastWAM states, adding donor-future/recipient-cache suppression while rerunning all four cells through explicit cache override. | The powered population job was running, but no powered result or partial causal metric had been inspected. The missing cell and cache-dominance prediction follow directly from the predeclared mechanism question and the pinned Optional-IDM architecture. | Factorial manifest `fastwam-kvfact-62d1420912418ade` registers 5,760 unique arms. It cannot start until the parent 8,640-arm matrix is complete. Exact parent replay and future-swap invariance are mandatory; the running parent manifest/enums/processes remain unchanged. |
| 2026-09-03T23:11:51Z | Supersede cache-factorial v1 with v2 manifest `fastwam-kvfact-ae55b7ed720094d4`, regenerating full-precision native future latents and video K/V from the already frozen video seeds instead of reconstructing K/V from the parent's FP16 storage artifact. | No cache-factorial output existed, and no partial powered causal metric had been inspected. A code audit found that the parent intentionally serializes future latents as FP16, which is not a sound source for the factorial's `1e-6` exact-replay gate. | States, seeds, 2x2 cells, 5,760 run count, outcomes, comparisons, and thresholds are unchanged. Each regeneration must reproduce the parent native action and the stored FP16 latent artifact within `1e-6`. V1 remains preserved as superseded pre-outcome; v2 manifest SHA-256 is `d6eb93b00af45ef7449247e7b038c8daa2907fdd25ed4ba4cb1e4275a95425b9`. |
| 2026-09-03T23:21:25Z | Add a leave-one-task-out descriptive robustness table to the frozen powered FastWAM analyzer. | The powered run was incomplete; no partial action or causal metric had been loaded or inspected. The original evaluation plan required checking that no single task drives the external replication. | The powered evidence gate, thresholds, hierarchical bootstrap, states, arms, and primary metrics are unchanged. The analyzer now emits each condition's state-mean metrics after excluding each of the 40 tasks in turn. |
| 2026-09-03T23:38:05Z | Supersede the powered retrieval-chance gate with analysis v2: combine four self-source diagonal cells and twelve donor-source off-diagonal cells into a balanced 4 by 4 future-source grid separately for latents and caches. | At freeze time 8,353/8,640 arm files and 115/120 state summaries existed, but no powered JSON/NPZ payload or causal metric had been opened. The donor-only arms contain only three admissible donor labels per recipient, so a 0.25 chance claim for that subset was not justified. | The primary source-retrieval gate now tests the suite-to-task-to-state hierarchical 95% lower bound of each balanced 16-cell grid against exact label-permutation chance 0.25. Donor-only retrieval remains secondary with a conservative 1/3 reference and the already frozen empirical wrong-latent/shuffled-cache contrasts. Manifest, states, seeds, arms, interventions, controls, and other gates are unchanged. Analysis-v2 protocol SHA-256 is `72b571d48494904c0f34d1334817cd59210736d0c97ff25e5cd255ad75e166c5`; analyzer SHA-256 is `ea0f7dde4278bd4904c0820f611f8b0902e4b1eebbf2b6d8a9c37b31958f65a3`. |
| 2026-09-03T23:48:00Z | Require a one-state excluded real-runtime gate before relaunching the FastWAM cache factorial. | The v2 powered-factorial launcher failed before inference because its loader namespace omitted `device`; an audit found 0 registered JSON arms, 0 NPZ arms, and 0 summaries. The correction adds only `device=args.device`. No factorial outcome existed or was inspected. | Run the unchanged 4-cell logic on manifest index zero of the previously completed 8-state smoke cohort using shard 0/8. Require exactly 48 finite arms, native/stored/base replay errors <=1e-6, and both same-cache future-swap invariances <=1e-6. Only a full pass permits freezing and launching powered v3; the excluded gate never enters scientific estimates. |
| 2026-09-03T23:52:00Z | Admit and freeze FastWAM powered cache-factorial v3 after the excluded real-runtime gate passed. | The predetermined excluded state produced all 48 arms with finite arrays; every native/stored/base replay and both same-cache future-swap maximum errors were exactly 0.0. The v2 powered attempt had produced no arm outcome. | V3 preserves all 120 powered states, 5,760 arms, four cells, seeds, outcomes, comparisons, thresholds, and hierarchical analysis. Manifest `fastwam-kvfact-a09195568dd5a17f` SHA-256 is `2cb76a6a4a6012caec72a976302f5dc91f75e15bb1dd9f3f4bc1699ae9a8f8fe`; it supersedes v2 solely to content-address the fixed runner and passed runtime gate. |
| 2026-09-04T00:07:00Z | Supersede held Cosmos archival manifests v3--v5 with immutable v6 after adversarial pre-outcome audit and excluded-runner checks. Define native-separation robustness on all 1,080 off-diagonal donor arms before within-state aggregation. | No archival evaluation state had been called. V3 failed provenance/statistical review; v4/v5 failed only excluded Bagels engineering gates before admitted output. The v6 excluded state then passed all 52-request replay, probe, fingerprint, coordinate, target, and Gaussian gates. | V6 pins the expected checkpoint probe and complete causal runtime closure; distinguishes transformed-input fingerprint from checkpoint identity; reports all metric denominators; freezes explicit evidence gates; uses pair-level separation quartiles with arm/state counts; and remains held for a fresh adversarial GO. This is a pre-outcome implementation/statistical correction, not an exploratory analysis. |
| 2026-09-04T00:31:00Z | Mark archival v6 permanently failed/incomplete and build v7 from a fresh root without changing or excluding any of its 90 frozen states. Replace the invalid final sampler-residual admission threshold with direct intervention-site fidelity audits at each active denoising call; retain final sampler max-absolute and L2 residuals as finite descriptive telemetry only. | V6 stopped at unit 19 after the single disclosed gate scalar 0.0334150791 exceeded 0.03. The 18 completed payloads were not opened or analyzed. Code review established that the failed scalar measured post-solver sampler drift rather than either actual causal write site. | V7 must require exact model-input future values and exact returned future velocities, requested/observed call and sigma agreement, mask/target/RNG provenance, per-call action nonwrite, and inactive no-op identity. All 90 states rerun; v6 outputs are never resumed or admitted. This is a pre-outcome manipulation-audit correction, not an exploratory analysis. |
| 2026-09-04T01:08:00Z | Bind final v7 and its excluded smoke to a full file-by-file checkpoint content manifest and independently verify the actual read-only `/checkpoint` mount before any admitted call. Keep the existing deterministic sampled-parameter probe separately labeled. | No v7 evaluation call existed. A prior audit correctly noted that a sampled parameter probe is not a full cryptographic checkpoint identity. A deterministic 87-file content manifest was available before the final v7 freeze. | Final v7 pins the content manifest, provenance audit, verifier, and successful `/checkpoint` verification receipt, and the launcher rehashes the checkpoint before its first evaluation call. The final manifest-bound excluded smoke must pass again before adversarial GO. |
| 2026-09-04T03:19:32Z | Admit one powered Cosmos 3 future-strength dose-response cohort under manifest `cosmos3-dose-c00d81db8d910603` after the prospective v2 amendment, exact excluded 92-call smoke, and signed independent GO. | No powered dose-response request or output existed. The excluded Bagels smoke passed all replay, endpoint, alpha-zero, live intervention-site, RNG, action-shape `[32,8]`, and action-nonwrite gates. | Run exactly the 30 frozen middle-phase archival states, 92 calls per state and 2,760 total, at alpha `{0,.25,.5,.75,1}`. Only the prespecified pairwise OLS-with-intercept trend and shared task-to-state bootstrap may support a positive linear-trend claim; strict monotonicity requires every adjacent hierarchical lower confidence bound above zero. |
