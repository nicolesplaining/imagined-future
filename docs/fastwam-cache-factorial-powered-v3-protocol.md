# FastWAM future-latent x video-cache factorial: powered v3

This v3 protocol supersedes powered v2 before any powered-factorial outcome was
generated. The scientific design, 120-state population, branch seeds, action
seeds, four factorial cells, outcomes, contrasts, exact tolerances, bootstrap,
and gate are unchanged.

## Mechanical runtime repair

The v2 launcher stopped before inference because the loader namespace omitted
the CLI device field. The output audit found zero registered JSON arms, zero NPZ
arms, and zero summaries. The v3 runner adds only `device=args.device` to that
namespace. Its SHA-256 is
`c3de7bae85d01411421fd5854fa0181d26621549774955d5c1b076923dcda357`.

## Required real-runtime admission gate

Before freezing v3, the fixed runner executed the identical four-cell logic on
manifest index zero of the previously completed, excluded smoke cohort:
`libero_spatial_task00_state000_wait30`. It produced 48/48 JSON arms, 48/48 NPZ
arms, and one summary; every stored array was finite. Native-action
regeneration, stored-latent regeneration, all three available base-reference
replays, recipient-cache future-swap invariance, and donor-cache future-swap
invariance each had maximum absolute error 0.0 (required <=1e-6). The excluded
summary SHA-256 is
`6ed0252f9c2e31f873115361e9e672286aa843577dc8d6f25d26e1be86e237ae`.

## Powered execution and analysis

Run all 120 states from powered parent manifest `fastwam-813f0233b9a2c083`,
using all twelve ordered recipient-to-donor pairs and four cells per pair:

- recipient future / recipient cache;
- donor future / recipient cache (suppression cell);
- recipient future / donor cache (rescue cell);
- donor future / donor cache.

The powered job contains 5,760 registered arms. Average ordered pairs within
state and compute 10,000 suite-to-task-to-state hierarchical bootstrap draws
with seed 20260903. Do not inspect a partial powered-factorial subset. Require
the same finite-array, parent replay, native regeneration, stored-latent replay,
and same-cache future-swap invariance checks used by the excluded admission
gate.

